{
  "name": "zync-workspace",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev:client":"cd client && pnpm dev",
    "dev:server":"cd server && pnpm exec nodemon src/index.js",
    "dev":"concurrently \"pnpm run dev:client\" \"pnpm run dev:server\"",
    "build":"pnpm install -r && pnpm run crypto:build && pnpm --dir client run build",
    "start":"pnpm --dir server exec node src/index.js",
    "crypto:build":"cd packages/zync-crypto && pnpm run build",
    "render:build":"pnpm install -r --no-frozen-lockfile && pnpm run crypto:build && pnpm --dir client run build && pnpm --dir server run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devEngines": {
    "packageManager": {
      "name": "pnpm",
      "version": "^11.1.2",
      "onFail": "download"
    }
  },
  "type": "module",
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "workspaces": [
    "packages/*"
  ]
}
